`sed(filePath, pattern, replacement, global?)` — Single-file regex substitution using Go regexp syntax. Use `replace` for multi-file recursive regex find-and-replace.

DEPRECATED: prefer the `replace` tool, which handles single-file and multi-file regex substitution. `sed` remains for backward compatibility and will be removed in a future release.
