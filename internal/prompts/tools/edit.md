`edit(filePath, oldString, newString, replaceAll?, edits?)` — Exact string replacement in an existing file with fuzzy-match fallback. Use `replace` for regex find-and-replace across multiple files; use `write` to create a new file; use `patch` to apply unified diff hunks.
