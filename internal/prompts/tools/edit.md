Performs exact string replacements in an existing file with fuzzy matching fallback. When the exact oldString is not found, the tool attempts fuzzy matching (ignoring whitespace differences). Supports batch edits via the edits[] array parameter for multiple replacements in a single call. Use replaceAll: true to replace all occurrences of oldString. Prefer edit over write when making targeted changes to existing files.
