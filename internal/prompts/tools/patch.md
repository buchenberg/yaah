`patch(patch, filePath?)` — Applies a unified diff patch to the given file. Patch can be a path to a `.patch` file or inline text. Use `edit` for exact string swaps; use `replace` for regex replacements.
