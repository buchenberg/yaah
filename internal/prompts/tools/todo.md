Creates and manages a structured todo list for the current session. Accepts an array of todos, each with `content` (description), `status` (pending, in_progress, completed, cancelled), and optional `priority` (high, medium, low). Replaces the entire list on each call — include all existing items you want to keep. Use for tracking progress through multi-step tasks during a session.
