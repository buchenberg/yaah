# Build output
/yaah
/dist/
*.exe
*.dll
*.so
*.dylib

# Test artifacts
*.test
*.out
coverage.txt
coverage.html

# Go workspace files (we use a single module)
go.work
go.work.sum

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local env / secrets (never commit)
.env
.env.local
*.local.yaml

# AI agent metadata dirs — never commit
.qwen/
.cursor/
.claude/
.continue/
.hermes/
.windsurf/
.copilot/
.aider/
.roo/
.codeium/
.tabnine/
.sourcegraph/
.ghost/
.kilo/
.pi/
.opencode/

# Gas Town runtime directories — never commit
mayor/
crew/
witness/
settings/
polecats/
.refinery/
.runtime/

# Local scratch / working dirs — never commit
.scratch/

**.exe~
**.dll~
**.so~
**.dylib~

# Debug and test files
*.log
state.db

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
.beads/.gt-types-configured
.beads/PRIME.md
